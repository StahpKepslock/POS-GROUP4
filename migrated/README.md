POS migration scaffold
======================

What I created

How this maps from the original C# project

Run (requires Java 11+ and Maven):

Open PowerShell and run:

```powershell
cd .\migrated\backend
mvn spring-boot:run
```

Then open http://localhost:8080 in your browser.

Credentials: admin / password (hardcoded in scaffold). Replace with real auth when ready.

Next steps
